import 'dotenv/config';import { QUEUE_NAMES } from '../queues/queue-names.mjs';import { startWorker } from './worker-runtime.mjs';import { getLogger } from '../services/logger.service.mjs';
await startWorker(QUEUE_NAMES.metrics,async(job)=>{(await getLogger()).info({jobId:job.id},'metrics placeholder acknowledged; legacy dashboards remain authoritative');return {delegatedToLegacy:true};},{concurrency:1});
